import ListsPageClient from "./ListsPageClient";

export const metadata = {
  title: "My Lists — CineScope",
  description: "Your personal movie and TV show collections.",
};

export default function ListsPage() {
  return <ListsPageClient />;
}
